# ── Training + data generation (chỉ cần khi train model) ────
-r requirements.txt
datasets>=2.18.0
accelerate>=0.28.0
evaluate>=0.4.0
scikit-learn>=1.4.0
openai>=1.30.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0
pyyaml>=6.0.0

# ── Export ONNX ───────────────────────────────────────────────
onnx>=1.16.0
onnxruntime>=1.18.0

# ── Optional: experiment tracking, notebooks ─────────────────
# wandb>=0.17.0
# ipykernel>=6.29.0
# matplotlib>=3.8.0
